- hosts: localhost
  connection: local

  roles:
    # 1. Common Base (Directory setup, etc.)
    - common
    - zsh

    # 2. Individual Roles (Refactored components)
    - fzf
    - nvim
    - wezterm

    # 3. Legacy Roles (For tools not yet refactored)
    #    Note: Do not remove yet; tools like neovim, wezterm, etc. are still handled here.
    - homebrew
    # - dotfiles
